# Browser Harness Skeleton

Phase 0/1 only adds the browser-side skeleton.

Planned usage:

- TypeScript wrappers around shared WGSL files
- static HTML pages that support non-interactive autorun modes
- headless Chrome checks similar to the existing browser WebGPU example
